#!/bin/sh

. config/options $1

mkdir -p $ADDON_BUILD/$PKG_ADDON_ID
  cp -PR $PKG_DIR/config/settings.xml* $ADDON_BUILD/$PKG_ADDON_ID/settings-default.xml

mkdir -p $ADDON_BUILD/$PKG_ADDON_ID/nfs-ganesha
#  cp -PR $BUILD/nfs-ganesha-*/* $ADDON_BUILD/$PKG_ADDON_ID/nfs-ganesha

